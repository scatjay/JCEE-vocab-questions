# 擱置意見清單

> 這份不是「被否決的垃圾桶」，是「現在不合適，但附一個明確的重新觸發條件」的清單。
> 跟 `kb/*.md` 的「被推翻的往下移標記，不刪除」是同一個精神，差別在：kb 記的是**單一代理人
> 自己的**判斷史；這份記的是**跨代理人、跨設計文件**、需要有人主動定期回頭檢查條件有沒有
> 滿足的擱置項——`cycle-designer` 每份裁決文件裡都有「未採納、什麼條件下我會改採納」這個
> 欄位，但那些條件目前散落在各份 `docs/cycle/*.md` 的散文裡，沒有人固定回頭檢查。
> 這份清單就是那個「回頭檢查」的固定落點。

## 格式

| # | 意見 | 誰提的／哪份文件 | 為什麼現在不合適 | 重新觸發條件 | 條件滿足時誰該重新評估 |

---

| 1 | WP-9 底線偵測安全網文案加碼校外心理支持資源（1980張老師專線） | `cycle-designer`（`docs/cycle/wp9-2-design.md` 候選B/C），楊老師 2026-09-14 否決 | 系統目前唯一能觀察到的訊號是「答題正確率」，這撐不起「這個學生需要心理支持」的推論——把學業困難跳接成心理健康議題是範圍蔓延，且對只是單純英文不好的學生可能造成不準確的標籤與驚嚇 | **不是「永遠不行」，是「現在的資料撐不起」**——若之後：(a) WP-6 老師報表成熟到能讓真人老師先看過反覆觸底的學生、由老師（不是系統）決定要不要主動關懷，或 (b) 系統新增了經過同意的、跟課業困難無關的獨立求助入口（不是被答題表現觸發），這個候選才重新有討論空間 | `wp-manager` 團隊健檢時檢查 WP-6 是否已經把「反覆觸底訊號」做進老師報表（見下方新增設計項）；一旦 WP-6 那部分上線，回頭問楊老師要不要重新考慮這個候選 |
| 2 | 精熟判定（政策8）整套換成連續衰減分數（借鏡FSRS的memory stability或BKT的P(mastery)後驗機率），取代離散的`consecutiveSpacedCorrect`計數器＋`masteredAt`/`demotedAt`時間戳判定 | `mutator`（`docs/cycle/mutation-wp8-mastery-continuous.md` 方案B），`cycle-designer` 2026-09-14 裁決不採納（`docs/cycle/MUTATIONS.md` #1） | 本計畫要求模型「一句話解釋得完」（政策3同精神先例），連續衰減分數比3次跨場次判定難解釋得多；FSRS/BKT這類模型的參數需要大量歷史複習資料校準，本專案目前0個真實學生資料，沒有東西可以校準衰減率；工程量牽動WP-1(計分)/WP-4(排程)/WP-6(報表)三個工作包，遠超這輪該承擔的範圍 | 有真實學生複習資料累積到可以校準衰減率參數時；或WP-6老師報表上線後真實觀察到「精熟後又忘記沒人接住」變成教師端反覆抱怨的具體訊號時（代表本輪採納的方案A撤銷機制證實不夠用） | `assessment-expert`真身＋`learning-scientist`真身聯合評估可行性與校準資料是否足夠，門檻高於政策3換Rasch模型（連精熟判定與複習排程的資料結構都要換），非`cycle-designer`單獨可裁決 |
| 3 | WP-9 觸底安全網目前把「誠實告知」與「降階路由」（wordwheel/roots）合併在同一個畫面，沒有實作 `docs/PLAN.md` §5 原本設想的第三層「連降階都撐不住時才升級文案／指向外部資源」——學生反覆觸底、在降階站也持續掙扎時，收到的訊息文案不會比第一次觸發更強烈 | `cycle-designer`（`docs/cycle/wp9-2-design.md` 2026-09-14 v2，english-teacher 審查候選A文案時整合裁決順帶發現） | 要偵測「降階目的地也失敗了」需要新增一層追蹤（學生在 wordwheel/roots 待了多久、答對率如何），目前 `evaluateFloorState` 沒有這個機制；這是本輪範圍外的新工程，且沒有真人測試資料顯示現有「同一句話重複顯示」不夠用 | 真人測試（或上線後真實學生資料）顯示：學生反覆觸底、在降階站持續掙扎，卻只收到同一句「下次上課去問老師」而沒有更強的升級語氣，導致該生實際上沒有真的去找老師求助 | `cycle-designer` 收到真人測試/實測訊號後，重新設計「降階也撐不住」的偵測條件與對應的升級文案，可能需要 `learning-scientist`／`ux-designer` 一起評估升級的時機與語氣 |
| 4 | 難度公式（WP-8 政策5／`docs/PLAN.md` §7 待決事項#7）換成 logistic/Rasch 模型，取代現行線性 `p_raw=0.5+(θ−d)` | `assessment-expert`（真身複審），`cycle-designer` 2026-09-14 裁決維持現行公式（`docs/cycle/wp8-2-design.md`「2026-09-14 v2 追記」） | 換模型解決不了 θ（個人小樣本、噪）與 d（全國大樣本、穩）直接線性相加的訊噪不對稱問題——這是被模擬版意見混在一起的獨立問題，換曲線形狀對它沒有幫助；公式失真最嚴重區域（`\|θ−d\|>0.5`）與選題排序鎖定區域（`θ−d≈0.35`附近）理論上互斥，clamp失真在決策相關範圍影響可忽略（**未經真實資料驗證**）；logistic的「一句話解釋」草稿比現行公式更難懂，違反本專案「一句話解釋得完」紅線；本專案目前0個真實學生資料，校準不了真正的Rasch模型 | (a) 累積到有真實學生複習資料，可以校準 logistic/Rasch 的參數；或 (b) 真人測試/實際資料顯示「`\|θ−d\|>0.5` 與選題排序區域理論上互斥」這個假設不成立（例如低正確率學生的 θ 剛好落在跟排序決策相關的範圍內、clamp 失真真的被學生/老師感受到） | `assessment-expert`（真身）重新評估可行性與校準資料是否足夠 |

---

## 使用方式

1. **新增擱置項**：任何代理人（多半是 `cycle-designer`，裁決衝突意見時）發現一個「現在不合適
   但條件變了可能該重新考慮」的意見，用 Edit 直接加一列，不用等 `wp-manager`。
2. **`wp-manager` 團隊健檢固定掃這份清單**（見它自己的 prompt「擱置意見健檢」一節）：
   逐條核對「重新觸發條件」現在是否已經滿足，滿足了就在回報裡點名、建議重新評估，
   不是自動變成動作——**重新評估的決定權還是在人／裁決角色，這份清單只負責不讓它被遺忘**。
3. **條件滿足後處理完的項目**：在該列後面加註「已於 YYYY-MM-DD 重新評估：<結果>」，
   不刪除整列——這樣之後查歷史看得出「這個想法曾經被認真考慮過兩次」。
