# 突變追蹤紀錄

> `mutator` 提案的完整生命週期：**後設分析 → 具體提案 → 裁決 → 觀察介入後的影響**。
> 目前的黑板/KB機制只涵蓋到「裁決」——沒有人固定回頭看「這個提案被採納之後，
> 團隊/系統真的變好了嗎」。這份清單就是補這一段。

## 為什麼分開記（不是全部塞進 mutator 的 kb）

- `kb/mutator.md`：mutator 自己的判斷力（哪類提案值得認真提）。
- 這份 `MUTATIONS.md`：**跨代理人共用的追蹤表**——`wp-manager` 定期回頭填「觀察到的影響」
  這一欄，這是它「討論」的具體落地方式：不是跟 mutator 即時對話（`MAX_DEPTH=1` 不允許），
  是**非同步**地在同一張表上留下反應，讓下一輪 mutator（或主線）讀得到。

## 格式

| # | 日期 | 用的框架 | 後設分析（這個框架看出什麼問題，不是解法本身） | 具體提案 | 裁決結果 | wp-manager 的討論回應 | 觀察窗口後的影響（至少間隔一輪其他實質工作後才填） |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-14 | Leveled Commitment Contracts（Sandholm & Lesser 1995） | WP-8政策8「精熟判定」三次修訂（v0連續2次→v1assessment-expert提「信心層級」被裁決不採納→v2現行3次跨場次+跨站分級）都停留在「離散狀態機要幾階、門檻多嚴」的框架內，從未問「精熟為何必須是離散狀態」。用leveled commitment的「承諾該不該允許事後付代價退出」鏡頭看，發現政策8有兩處「全有全無承諾」從未被質疑：(1)`consecutiveSpacedCorrect`一答錯就歸零，等於整組證據作廢而非部分履約；(2)`masteredAt`一旦設定沒有任何撤銷機制，政策文字完全沒討論「精熟後又忘記/答錯」會發生什麼——這與政策4（間隔複習排程本身承認遺忘隨時間累積）互相矛盾。 | 方案A（建議現在採納，`cycle-designer`可直接裁決）：答錯時`consecutiveSpacedCorrect`扣1而非歸零；`masteredAt`/`crossStationVerified`在下次到期複習答錯時可被撤銷並記錄「曾精熟後退步」供老師報表呈現。不動schema、不動3次跨場次規則本身。方案B（不建議這輪採納，僅記錄存在）：整套換成連續衰減分數（借鏡FSRS/BKT），解決政策4/8時間假設矛盾與「n設多少」的參數之爭，但違反「一句話解釋得完」門檻、缺校準資料、牽動WP-1/WP-4/WP-6三個工作包，代價遠大於現階段（0個真實學生、900天餘裕）該承擔的範圍。 | `cycle-designer` 2026-09-14 裁決：**採納方案A**，含wp-manager補的兩點修正——(1) `consecutiveSpacedCorrect`改為獨立事件答對+1(上限3)/答錯-1(下限0)，不再整組歸零；(2) 新增schema欄位`demotedAt`（`masteredAt`保留原值不清空，記錄「曾精熟」的歷史事實），已達成精熟門檻的KC其後第一次獨立事件答錯即撤銷（`consecutiveSpacedCorrect`由3降到2），可重新達成。已同步改寫`docs/PLAN.md`§5 WP-8政策8全文、schema、驗收標準第7條，並新增驗收標準第15、16條、擴大第13條紅線驗收涵蓋範圍。方案B（連續衰減分數）**維持不建議這輪採納**的判斷，同意mutator的理由（可解釋性門檻、無校準資料、跨WP工程量），已列入`docs/cycle/PARKED.md` #2，重新觸發條件：有真實學生複習資料可校準參數、或報表證實「精熟後又忘記沒人接住」變成真實抱怨時重新評估。對新流程本身的評論：wp-manager補的兩點都是**真的省了裁決者重新發現的功夫**，不是形式性轉發——(1) schema自相矛盾（方案A原文自稱『不需要新欄位』卻要求記錄撤銷歷史）多半要等我實際動手寫schema那一步才會撞到，wp-manager先抓到讓我少走一輪來回；(2) 驗收標準第7條會被新行為打臉這件事，若沒被提醒，第一版裁決文字很可能只改政策文字、漏改機械驗收，要等build階段或下一輪健檢才會被發現——這正是本專案『驗收標準要可驗證、可執行』精神最容易破功的地方，這層審查有實質攔截到一個原本會漏掉的缺口。 | **[wp-manager 2026-09-14]** 框架選得準：核對PLAN.md §5 WP-8政策8原文與wp8-2-design.md「未採納」清單第2點，三次修訂（v0連續2次→v1「信心層級」被裁決不採納→v2現行3次跨場次+跨站分級）確實從未離開「離散狀態要幾階」的框架，後設分析點出的兩處全有全無承諾（`consecutiveSpacedCorrect`歸零、`masteredAt`無撤銷）是政策文字裡真實存在的空白，不是套框架硬找的；政策4/8對「時間如何影響記憶」互相矛盾這點我核對過也成立，是個沒人問過的縫，這條後設分析我認同。方案A方向同意、建議可交`cycle-designer`裁決，但發現方案A自己文件裡有兩處遺漏、裁決前該一併帶上：(1) PLAN.md §5 WP-8驗收第7條「造到期複習中間有一次答錯的假學生，驗證`consecutiveSpacedCorrect`歸零重算」是已定案的機械驗收文字，方案A把歸零改成扣點後，這條驗收標準的字面會直接跟新行為矛盾，需要同步改寫成「驗證扣1而非歸零」，方案A文件沒提到這條連動，不能只改判定邏輯不改對應的驗收文字。(2) 方案A宣稱「Phase 0 schema...完全夠用，不需要新欄位」，但同時要求「記錄撤銷事件供老師報表顯示曾精熟後退步」——現行schema `masteredAt: ms\|null` 只是單一值、沒有history，撤銷（設回null或降級）後「曾經精熟過」這件事本身在資料庫裡就會消失、沒東西可顯示，這跟「不需要新欄位」自相矛盾，實務上至少需要一個新欄位（例如`demotedAt`或把`masteredAt`改記陣列）。這兩點不是要打回重寫，建議連同方案A一起送`cycle-designer`裁決時附上，一次裁完，不必再跑一輪mutator。方案B「這輪不建議採納」的判斷我同意，理由（可解釋性門檻、無校準資料、跨WP工程量）紮實且跟政策3先例一致，沒有意見要補充。 | |

（第一筆已於本輪填入，見上表 #1；完整推演見 `docs/cycle/mutation-wp8-mastery-continuous.md`）

## 使用方式

1. **mutator 提案時**：新增一列，「後設分析」欄要能獨立成立——即使裁決者不同意具體提案，
   也該看得懂「用這個框架看，這裡確實有一個值得問的問題」。後設分析跟具體提案分開寫，
   避免「框架只是包裝、其實一開始就想好答案」。
2. **裁決者（`cycle-designer`／楊老師）處理完提案後**：回填「裁決結果」欄。
3. **`wp-manager` 每輪健檢**（見它自己 prompt 的「突變討論與追蹤」一節）：
   - 對**還沒被自己討論過**的提案：寫一段實質回應到「wp-manager 的討論回應」欄——
     不是「已轉交裁決者」這種轉發式的話，是真的讀完後設分析與提案，給出「這個角度我同意/
     不同意什麼、還缺什麼」的具體意見。這欄有內容之後，下次呼叫 mutator 時可以把它當輸入，
     讓 mutator 回應（形成非同步的來回，不是即時對話）。
   - 對**已裁決、且已經過至少一輪其他實質工作**的提案：回填「觀察到的影響」——用客觀證據
     （git log、黑板事件、下一輪相關產物），不要憑印象；還太早看不出影響就寫「觀察窗口未到，
     暫不填」，不要為了有東西寫而編。
4. **影響是負面或中性時，不要淡化**——如果一個被採納的突變提案事後看起來沒有實質幫助、
   甚至讓事情變複雜，如實記下來。這本身是「這類框架/這種提案在本專案有沒有用」的重要資料，
   之後 `mutator` 的 KB 該累積這個教訓。
